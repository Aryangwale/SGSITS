<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    product page

    <% if (result) { %>
        <% result.forEach(function(user){ %>
            <h2><%= user.id %>&nbsp;&nbsp;&nbsp;<%= user.product_name %></h2>
          <% }); %>
      <% } %>
</body>
</html>